# 班级喊话系统 · 使用说明(Mac 演示版)

> 这套系统复刻自《班级喊话使用手册》:老师在**手机或电脑网页**上发一条通知,绑定的 **Mac 大屏(教室演示屏)** 会自动**全屏弹窗 + 中文语音播报**。
> 适合在教室、办公室或展会大屏上演示、真实使用。

---

## 一、这套系统由三部分组成

| 部分 | 谁来用 | 在哪 | 干什么 |
|---|---|---|---|
| 网页端(老师端) | 老师 / 办公室 | 手机、电脑浏览器 | 登录、管理班级、发送通知 |
| 教室演示屏(教室端) | 教室电脑 / 你的 Mac | Mac 上的桌面窗口 | 接收通知,全屏弹窗 + 语音朗读 |
| 后台服务 | 无感 | 本机自动运行 | 连接上面两端的"中转站" |

> 本演示版里,服务就运行在这台 Mac 上,手机和 Mac 只要在**同一个 Wi-Fi** 就能互相通信。

---

## 二、启动前准备(仅第一次需要)

在本项目根目录和 `mac-screen` 目录各执行一次依赖安装:

```bash
npm install
cd mac-screen
npm install
```

---

## 三、日常怎么启动(三步)

### 第 1 步:启动后台服务

```bash
npm start
```

启动成功后终端会打印:

```
演示账号  ->  用户名: demo  密码: demo123
教室绑定码 ->  DEMO8YQZ
可用演示激活卡: OWN-XXXX-XXXX(普通), CO-XXXX-XXXX(协同) ...
```

> 小提示:Mac 演示屏能自动帮你拉起服务,如果嫌麻烦,第 1 步也可以省略。

### 第 2 步:打开 Mac 教室演示屏(全屏)

```bash
cd mac-screen
npm start
```

你的 Mac 会弹出一个**全屏窗口**,即教室大屏。
它会自动连接服务;若服务没启动,它会自动帮你在后台拉起。

### 第 3 步:手机打开老师端网页

1. 手机连上**和 Mac 同一个 Wi-Fi**;
2. 用浏览器打开:`http://<Mac的IP>:3000`(IP 查询方法见下方);
3. 登录 `demo / demo123` 即可发送通知。

**怎么查 Mac 的 IP**(IP 每次连不同网络会变,换网络后需重查):

```bash
ipconfig getifaddr en0
```

---

## 四、第一次使用建议走一遍完整流程(约 2 分钟)

1. **绑定演示屏**:在 Mac 全屏窗口输入绑定码 `DEMO8YQZ` → 点「连接并开始接收通知」;
   页面会变成"正在监听通知"的待机大屏(显示时钟)。
   - 如果听不到声音,先点一下屏上的 **「🔊 点击此处开启语音播报」**(浏览器安全限制,需手动解锁一次),再点 **「测试播报」** 试声音。
2. **手机发送**:手机网页登录 `demo / demo123` → 进入「后台管理」→ 选「示例班级·三2班」→ 输入内容(如:请各班班长到会议室开会)→ 发送。
3. **看效果**:Mac 大屏立即**全屏弹出通知**并**中文朗读**;稍后自动关闭,回到待机大屏。

> 也可以两个浏览器标签页模拟:一个开 `http://localhost:3000/app`,另一个开 `http://localhost:3000/room`。

---

## 五、用你自己的账号(不依赖 demo)

1. 在登录页点 **注册**;
2. 把终端打印的**普通激活卡号**(`OWN-` 开头)填进去,设置账号和密码;
   - `OWN-` 普通卡:自带 **2 个班级**名额;
   - `CO-` 协同卡:只能被邀请加入别人的班级,不能自己建班;
3. 登录后新建班级 → 系统生成 8 位**绑定码** → 用该码绑定演示屏;
4. 以后每次发送都选这个班级即可。绑定一次,下次开屏自动连接。

**忘记密码**:登录页 → 忘记密码 → 输入用户名 + **当初注册时用的激活卡号** 即可重设。

---

## 六、手机"添加到主屏幕"(把它变成 App 图标)

**iPhone**:用 Safari 打开老师端网址 → 点底部「分享」→「添加到主屏幕」→ 桌面出现图标,点开就像原生 App,全屏无地址栏。

**安卓**:Chrome 打开网址 → 菜单 →「添加到主屏幕」或「安装应用」。

(后台页面已适配手机屏幕。)

---

## 七、Mac 演示屏日常操作

| 操作 | 方法 |
|---|---|
| 退出演示屏 | 点窗口右上角 **「⏻ 退出演示屏」**(只在桌面版出现),或按 `Command + Q` |
| 解绑 / 换班级 | 右上角 **「解绑」**,回到绑定页输入新绑定码 |
| 全屏切换 | 右上角 **「⛶ 全屏」** |
| 试声音 | 右上角 **「测试播报」** |
| 静音 | 右上角 **「🔇 声音关」** |
| 播放通知时手动关闭 | 通知弹层右上角 **✕** |

- 绑定码会保存在这台 Mac 上,**重启电脑后再次打开演示屏会自动重连**,无需重新输入。
- 如果老师在后台**重新生成了绑定码**,演示屏会自动跳回绑定页,需输入新码。

---

## 八、演示速查卡(贴在旁边最方便)

```
老师端网址(手机):  http://192.168.31.179:3000    ← IP 按实际查询
演示账号:          demo  /  demo123
演示绑定码:        DEMO8YQZ
演示班级:          示例班级·三2班
```

---

## 九、数据重置(演示完恢复原样)

想清空所有测试数据、回到最初状态:

```bash
rm -rf data        # 删除数据库文件(仅本演示项目数据)
npm start          # 重新启动,自动重建干净数据
```

---

## 十、常见问题

**手机打不开老师端网页?**
- 确认手机和 Mac 在同一个 Wi-Fi;
- 确认输入的 IP 是当前网络的(Mac 上 `ipconfig getifaddr en0` 重新查);
- 换网络后 IP 会变,后台服务一般无需重启,但演示屏自动连接的地址仍有效(它连的是本机)。

**有弹窗但没声音?**
- 首次需在演示屏上点一次 **「开启语音播报」** 按钮(Mac 系统语音里需已启用中文「普通话」);
- 确认演示屏右上角是「🔊 声音开」而不是「🔇 声音关」;
- 发送通知时后台别勾掉"语音播报"开关。

**演示屏黑屏 / 不显示?**
- 演示屏已内置"禁用硬件加速",个别机型仍异常时可重启一次 `mac-screen`;
- 关掉演示屏窗口再 `npm start` 一次。

**想在另一台 Mac 或发给别人用?**
- 把 `mac-screen` 打包成独立 App:在 `mac-screen` 目录先 `npm install -D electron-builder`,再 `npm run dist`,产物在 `dist/`。
- 但**服务端仍在发起演示的那台机器上**,打包的 App 需能访问到那台机器的 IP(用 `ROOM_URL=http://<服务IP>:3000/room npm start` 指定)。
- 若需"App 装在哪都能用",需把服务部署到公网/云服务器(可再帮你配置)。

---

*本说明对应"班级喊话系统(复刻演示)"的本地 Mac 演示版。删除 `data` 或重新 `npm start` 后,终端里打印的激活卡号会重新生成,请以当次打印为准。*
